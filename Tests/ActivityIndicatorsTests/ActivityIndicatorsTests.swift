import XCTest
@testable import ActivityIndicators

final class ActivityIndicatorsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(ActivityIndicators().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
